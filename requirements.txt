# =================================================================
# B MANT COM 601 - SISTEMA DE PREDICCIÓN DE REPUESTOS E INSUMOS
# Dependencias de Producción (Optimizado, Portátil y Auto-resoluble)
# =================================================================

# --- Frameworks Principales ---
fastapi>=0.115.0          # Versión moderna para evitar conflictos de Starlette con Streamlit
uvicorn[standard]>=0.30.0 # Servidor ASGI para correr el Backend
streamlit>=1.59.2         # Interfaz de Usuario Interactiva (Frontend)

# --- Procesamiento de Datos y Drivers ---
pandas>=2.2.1             # Manipulación y análisis de datos
openpyxl>=3.1.2           # ¡INDISPENSABLE! Driver para que Pandas procese archivos Excel (.xlsx)
numpy>=1.26.4             # Soporte para arrays y cálculo numérico computacional
pyarrow>=15.0.0           # Motor de procesamiento eficiente de datos para Pandas
psycopg2-binary>=2.9.9    # Conector nativo para Base de Datos PostgreSQL

# --- Base de Datos Vectorial e Inteligencia Artificial (Brain) ---
qdrant-client>=1.8.0      # Cliente para la BD Vectorial e indexación del RAG
tenacity>=9.1.4           # Manejo robusto de reintentos para llamadas de APIs a LLMs

# --- Utilidades, Estructura y Seguridad ---
python-dotenv>=1.2.2      # Gestión segura de variables de entorno (.env / .env.local)
pydantic>=2.6.4           # Validación estricta y tipado de datos para las APIs (Schemas)
pydantic-settings>=2.2.1  # Gestión de configuraciones del sistema basadas en Pydantic
python-multipart>=0.0.32  # Requerido por FastAPI para procesar formularios y carga de archivos
requests>=2.34.2          # Cliente HTTP para peticiones externas
pillow>=12.3.0            # Driver para procesamiento y visualización de imágenes
GitPython>=3.1.51         # Automatización y control del repositorio Git corporativo desde Python
fpdf2>=2.4.2              # Librería para generar documentos PDF